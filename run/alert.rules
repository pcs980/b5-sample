groups:
  - name: app_alerts
    rules:
      - alert: Unhealthy
        expr: dependency_up == 0
        for: 20s
        labels:
          severity: critical
        annotations:
          summary: Host {{ $labels.instance }} of {{ $labels.job }}
